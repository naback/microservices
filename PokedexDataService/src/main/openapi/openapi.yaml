openapi: "3.0.3"
info:
  description: "This is a training exercise for a Pokedex server. For this sample, you can use the api key `special-key` to test the authorization filters."
  version: "1.0.0"
  title: "Swagger Pokedex"
  termsOfService: "http://swagger.io/terms/"
  contact:
    email: "apiteam@swagger.io"
  license:
    name: "Apache 2.0"
    url: "http://www.apache.org/licenses/LICENSE-2.0.html"
tags:
  - name: "pokemon"
    description: "everything about your pokemons"
    externalDocs:
      description: "Find out more"
      url: "http://swagger.io"
paths:
  /pokemon:
    post:
      tags:
        - "pokemon"
      summary: create a pokemon object in database
      operationId: createPokemon
      parameters:
        - in: header
          name: apiKey
          description: the unique api key for this server
          schema:
            type: string
          required: true
      requestBody:
        description: create pokemon request
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePokemonRequest'
        required: true
      responses:
        200:
          description: success response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePokemonResponse'
        400:
          description: bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorInfo'
components:
  schemas:
    CreatePokemonRequest:
      type: object
      required:
        - parametro1
      properties:
        parametro1:
          $ref: '#/components/schemas/parametro1'

    CreatePokemonResponse:
      type: object
      required:
        - parametro1
      properties:
        parametro1:
          $ref: '#/components/schemas/parametro1'

    ErrorInfo:
      type: object
      required:
        - parametro1
      properties:
        parametro1:
          $ref: '#/components/schemas/parametro1'

    parametro1:
      type: string
      description: primeiro parametro
      example: 'xalala'

                